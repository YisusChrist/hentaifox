version: 1
services:
  - type: web
    name: your-app-name
    runtime: python
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    autoDeploy: true
    region: oregon  
    
